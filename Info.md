# 🎓 Как восстановить биты приватного ключа из результатов атаки ZVP-GLV

## 📚 Введение для школьников

Привет! Эта инструкция объяснит тебе, как понять результаты атаки ZVP-GLV и восстановить биты приватного ключа. Мы будем говорить простым языком и разберем все по шагам.

### 🔑 Что такое приватный ключ?

Приватный ключ в криптографии - это секретное число, которое позволяет тебе подписывать транзакции или расшифровывать сообщения. В Bitcoin это очень большое число (256 битов).

### 🎯 Что делает атака ZVP-GLV?

Атака пытается угадать **верхние биты** (самые важные цифры) приватного ключа, используя специальные математические трюки.

## 📊 Понимание результатов атаки

### Шаг 1: Смотрим на результат атаки

После запуска атаки ты получишь что-то вроде:

```
📊 Подробные результаты атаки:
   Восстановлено битов: 6.58
   Найдено кандидатов: 3
   
   Кандидаты скаляров (k0, k1):
     1. k0=[1, 1, -1, 1], k1=[1, -1, 1, 1]
     2. k0=[1, 1, -1, -1], k1=[1, -1, 1, -1]
     3. k0=[-1, 1, 1, 1], k1=[-1, -1, -1, 1]
```

### Шаг 2: Что означают эти числа?

- **k0 и k1** - это две части приватного ключа в специальном GLV представлении
- **[1, 1, -1, 1]** - это биты в GLV-SAC кодировании:
  - `1` означает "+1"
  - `-1` означает "-1" 
  - `0` означает "0" (редко встречается)
- Биты идут **от старших к младшим** (слева направо)

### Шаг 3: Как работает GLV-декомпозиция?

В кривой secp256k1 любой приватный ключ `d` можно записать как:
```
d = k0 + k1 × λ (mod n)
```

Где:
- `k0` и `k1` - это примерно половинки приватного ключа
- `λ = 78074008874160198520644763525212887401909906723592317393988542598630163514318`
- `n = 115792089237316195423570985008687907852837564279074904382605163141518161494337` (порядок кривой)

## 🔨 Пошаговое восстановление битов

### Шаг 1: Преобразуем GLV-SAC в обычные биты

GLV-SAC кодирование означает:
- `(k0[i], k1[i]) ∈ {(1,0), (-1,0), (1,1), (-1,-1)}`

Пример: `k0=[1, 1, -1, 1], k1=[1, -1, 1, 1]`

**Интерпретация по битам:**
- Бит 0 (старший): `(k0=1, k1=1)` означает `d = k0 + k1×λ = 1 + 1×λ`
- Бит 1: `(k0=1, k1=-1)` означает `d = 1 + (-1)×λ = 1 - λ`  
- Бит 2: `(k0=-1, k1=1)` означает `d = -1 + 1×λ = λ - 1`
- Бит 3 (младший): `(k0=1, k1=1)` означает `d = 1 + 1×λ`

### Шаг 2: Вычисляем числовые значения

Для каждого кандидата нужно вычислить:

```python
# Пример для k0=[1, 1, -1, 1], k1=[1, -1, 1, 1]
# Биты идут от старшего к младшему

k0_value = 1×2³ + 1×2² + (-1)×2¹ + 1×2⁰ = 8 + 4 - 2 + 1 = 11
k1_value = 1×2³ + (-1)×2² + 1×2¹ + 1×2⁰ = 8 - 4 + 2 + 1 = 7

# Итоговый приватный ключ (только верхние биты):
d_partial = k0_value + k1_value × λ (mod n)
```

### Шаг 3: Понимаем ограничения

⚠️ **ВАЖНО**: Атака восстанавливает только **верхние биты**!

Если атака нашла 4 бита, то:
- Из 256 битов приватного ключа мы знаем только 4 верхних
- Остальные 252 бита остаются неизвестными
- Это все равно очень ценная информация для криптоанализа!

## 🧮 Математические детали

### Формула восстановления

Для кандидата `(k0_bits, k1_bits)` длины `t` битов:

```python
def recover_partial_key(k0_bits, k1_bits, lambda_val, n):
    """
    Восстанавливает частичный приватный ключ из битов GLV-SAC
    
    Args:
        k0_bits: список битов k0 (от старшего к младшему)
        k1_bits: список битов k1 (от старшего к младшему)  
        lambda_val: значение λ для кривой secp256k1
        n: порядок кривой
    
    Returns:
        Частичное значение приватного ключа (только верхние биты)
    """
    t = len(k0_bits)
    
    # Преобразуем биты в числа
    k0 = sum(k0_bits[i] * (2 ** (t-1-i)) for i in range(t))
    k1 = sum(k1_bits[i] * (2 ** (t-1-i)) for i in range(t))
    
    # Вычисляем частичный ключ
    partial_key = (k0 + k1 * lambda_val) % n
    
    return partial_key, k0, k1
```

### Диапазон возможных ключей

Если мы восстановили `t` верхних битов, то полный приватный ключ лежит в диапазоне:

```
d_min = partial_key × 2^(256-t)
d_max = (partial_key + 1) × 2^(256-t) - 1
```

## 📝 Практический пример

Допустим, атака нашла кандидата:
- `k0 = [1, -1, 1]` (3 бита)
- `k1 = [1, 1, -1]` (3 бита)

**Шаг 1**: Преобразуем в числа
```
k0_value = 1×4 + (-1)×2 + 1×1 = 4 - 2 + 1 = 3
k1_value = 1×4 + 1×2 + (-1)×1 = 4 + 2 - 1 = 5
```

**Шаг 2**: Вычисляем частичный ключ
```
λ = 78074008874160198520644763525212887401909906723592317393988542598630163514318
d_partial = (3 + 5 × λ) mod n
```

**Шаг 3**: Понимаем диапазон
```
# У нас есть 3 верхних бита из 256
# Остальные 253 бита неизвестны
d_min = d_partial × 2^253
d_max = (d_partial + 1) × 2^253 - 1
```

## 🎯 Практические советы

### 1. Проверка результатов
- Всегда проверяй все кандидаты
- Правильный кандидат должен давать известный публичный ключ
- Используй скрипт проверки (см. `key_checker.py`)

### 2. Понимание качества атаки
- **Много кандидатов** (>100) = плохо, мало информации
- **Мало кандидатов** (<10) = хорошо, много информации  
- **1 кандидат** = идеально, уникальное решение

### 3. Увеличение эффективности
- Больше битов = больше вычислений, но больше информации
- Начинай с малого количества битов (4-6)
- Постепенно увеличивай если нужно больше точности

## ⚠️ Этические соображения

**ПОМНИ**: Эта информация только для обучения!

- ✅ Изучай криптографию для понимания безопасности
- ✅ Тестируй только свои собственные ключи
- ✅ Помогай улучшать криптографические системы
- ❌ НЕ атакуй чужие ключи без разрешения
- ❌ НЕ используй для кражи криптовалют
- ❌ НЕ нарушай законы своей страны

## 📚 Дополнительное чтение

- [Оригинальная статья GLV](https://www.iacr.org/archive/crypto2001/21390189.pdf)
- [Документация secp256k1](https://en.bitcoin.it/wiki/Secp256k1)
- [Основы эллиптических кривых](https://andrea.corbellini.name/2015/05/17/elliptic-curve-cryptography-a-gentle-introduction/)

---

*Помни: знание - это сила, но с большой силой приходит большая ответственность! 🕷️*